# Site settings
title: "Page Title"
description: "This is a frame for a Jekyll website"
keywords:
- tedor, tedör, krisztian hofstadter, creative technologist

# url
url: # might not be needed;
baseurl: "" # required if used under gh-pages branch, else (under master branch) just leave empty/blank

# Build settings
permalink: /:title/
## exclude
exclude:
- README.md

# Front Matter Defaults
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
